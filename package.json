{
  "name": "sai-moderation-docker",
  "private": true,
  "type": "module",
  "version": "0.0.0-development",
  "description": "Streamer.bot moderation bridge with Docker-first development workflow.",
  "scripts": {
    "lint": "eslint src/**/*.js tests/**/*.js",
    "format": "prettier --write \"src/**/*.js\" \"tests/**/*.js\" \"docs/**/*.md\" \"*.md\" \".github/**/*.yml\"",
    "format:check": "prettier --check \"src/**/*.js\" \"tests/**/*.js\" \"docs/**/*.md\" \"*.md\" \".github/**/*.yml\"",
    "test": "vitest run",
    "check": "npm run lint && npm run format:check && npm run test"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.4",
    "conventional-changelog-conventionalcommits": "^9.3.1",
    "eslint": "^8.57.1",
    "prettier": "^3.6.2",
    "semantic-release": "^24.2.7",
    "vitest": "^2.1.9"
  }
}
